@model List<CardData>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

   @*  <body>
        <div class="container mt-5">
            <h6>Dashboard</h6>
            <div class="card-container">
                @foreach (var item in Model)
                {
                    <a href="@Url.Action("Details", "Dashboard", new { title = item.Basic_Information.Title })">
                        <div class="card p-3">
                            <h5 class="card-title">@item.Basic_Information.Title</h5>

                        </div>
                    </a>
                }
            </div>
        </div>
        </div>
    </body>
</html> *@
</head>
<body>


<div class="mb-2">


    <h4 class="text-left mb-4">My Deals</h4>
    <div class="container">
        <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
            @foreach (var item in Model)
            {
                <div class="col">
                    <div class="card" style="background-color: #f8f2f2; border: 1px solid #901F17;">
                        <div class="card-header py-3 d-flex align-items-center justify-content-between" style="background-color: #901F17; color: white;">
                            <h5 class="d-flex align-items-center" style="font-size: 18px; font-weight: bold; color:#fff">
                                @item.Basic_Information.Title
                            </h5>
                            <a href="@Url.Action("Details", "Home", new { id = item.Basic_Information.id })">
                                <i class='fas fa-edit' style="color:#fff"></i>
                            </a>
                        </div>
                        <div class="card-body py-3 border-bottom d-flex justify-content-between">
                            <div class="d-flex flex-column">
                                <p class="d-block mb-2">
                                    <i class='fas fa-user-plus'></i> Team Members : @item.Additional_Info.Number_Of_Movers
                                </p>
                                <p class="d-block mb-0">
                                   @*  <i class="fa fa-calendar"></i> Moving Date : @item.Moving_Date.moving_to_date *@
                                <i class="fa fa-calendar"></i> Moving Date : 12-Dec-24
                                </p>
                            </div>

                            <div>
                                <p class="mb-0 h4">
                                    @item.Basic_Information.Status
                                </p>
                            </div>
                        </div>
                      @*   <div class="card-footer bg-light py-3 border-bottom d-flex align-items-center justify-content-between">
                            <div><button class="btn btn-light-success btn-sm"><i class="fas fa-eye me-1"></i>View</button></div>
                        </div> *@
                    </div>
                </div>
            }
        </div>
    </div>
</div>
</body>
</html>